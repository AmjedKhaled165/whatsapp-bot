# 🤖 Royal WPPConnect - بوت واتساب احترافي

مشروع كامل وجاهز لإنشاء بوت واتساب باستخدام WPPConnect مع API متكامل.

## ✨ المميزات

- ✅ **جلسة دائمة** - تحتاج QR Code فقط في أول تشغيل
- 📱 **رسائل نصية** - إرسال واستقبال رسائل
- 🖼️ **صور وملفات** - إرسال الوسائط المتعددة
- 🤖 **ردود تلقائية** - رد آلي على رسائل محددة
- 🌐 **REST API** - واجهة برمجية كاملة
- 📡 **Webhook** - استقبال البيانات من خوادم خارجية

---

## 🚀 التثبيت والتشغيل

### 1️⃣ تثبيت المكتبات

```bash
npm install
```

### 2️⃣ تشغيل السيرفر

```bash
npm start
```

أو

```bash
node server.js
```

### 3️⃣ مسح QR Code (أول مرة فقط)

- ستظهر لك رسالة في الكونسول مع QR Code
- افتح واتساب على موبايلك
- اذهب إلى: **الإعدادات** → **الأجهزة المرتبطة** → **ربط جهاز**
- امسح الـ QR Code

✅ **بعد المسح الأول، لن تحتاج QR مرة أخرى!**

الجلسة ستحفظ تلقائيًا في مجلد `tokens/`

---

## 📡 API Endpoints

السيرفر يعمل على: `http://localhost:3000`

### 1. التحقق من حالة البوت

```http
GET /status
```

**الرد:**
```json
{
  "status": "ready",
  "message": "البوت يعمل بنجاح ✅",
  "session": "royal_session"
}
```

---

### 2. إرسال رسالة نصية

```http
POST /send
Content-Type: application/json
```

**Body:**
```json
{
  "number": "9665XXXXXXXX",
  "message": "مرحباً! هذه رسالة تجريبية 👋"
}
```

**مثال باستخدام cURL:**
```bash
curl -X POST http://localhost:3000/send \
  -H "Content-Type: application/json" \
  -d '{"number":"9665XXXXXXXX","message":"مرحباً من WPPConnect!"}'
```

**الرد:**
```json
{
  "status": "sent",
  "to": "9665XXXXXXXX@c.us",
  "message": "مرحباً! هذه رسالة تجريبية 👋"
}
```

---

### 3. إرسال صورة

```http
POST /send-image
Content-Type: application/json
```

**Body:**
```json
{
  "number": "9665XXXXXXXX",
  "imageUrl": "https://example.com/image.jpg",
  "caption": "صورة جميلة 📸"
}
```

---

### 4. إرسال ملف

```http
POST /send-file
Content-Type: application/json
```

**Body:**
```json
{
  "number": "9665XXXXXXXX",
  "fileUrl": "https://example.com/document.pdf",
  "fileName": "document.pdf"
}
```

---

### 5. Webhook (استقبال البيانات)

```http
POST /webhook
Content-Type: application/json
```

يمكنك استخدام هذا الـ endpoint لاستقبال بيانات من خوادم خارجية.

---

## 🤖 الردود التلقائية

البوت يحتوي على ردود تلقائية مبرمجة مسبقًا:

| الرسالة المستقبلة | الرد التلقائي |
|-------------------|---------------|
| `مرحبا` أو `hi` | أهلاً وسهلاً! 👋<br>كيف يمكنني مساعدتك؟ |
| `معلومات` | معلومات عن البوت وإمكانياته |
| `وقت` | الوقت الحالي |

يمكنك تعديل هذه الردود أو إضافة ردود جديدة في ملف `server.js`

---

## 📁 هيكل المشروع

```
royal-wppconnect/
│
├── server.js          # الملف الرئيسي
├── package.json       # المكتبات والإعدادات
├── README.md          # هذا الملف
│
└── tokens/            # مجلد الجلسات (يُنشأ تلقائيًا)
    └── royal_session/ # جلسة واتساب المحفوظة
```

---

## 🔧 التخصيص

### تغيير اسم الجلسة

في `server.js`، سطر رقم 12:

```javascript
session: 'royal_session',   // غيّر الاسم هنا
```

### تغيير المنفذ (Port)

في `server.js`، سطر رقم 225:

```javascript
const PORT = process.env.PORT || 3000;  // غيّر الرقم هنا
```

أو قم بتعيين متغير البيئة:

```bash
PORT=5000 node server.js
```

---

## 🛠️ إضافة ردود تلقائية جديدة

في `server.js`، داخل دالة `client.onMessage`:

```javascript
// مثال: رد على كلمة "السعر"
if (message.body.toLowerCase() === "السعر") {
    await client.sendText(message.from, "الأسعار تبدأ من 100 ريال 💰");
}
```

---

## 📝 ملاحظات مهمة

- ⚠️ **لا تشارك مجلد `tokens/`** - يحتوي على بيانات جلستك الخاصة
- 🔒 يُنصح بإضافة `tokens/` إلى `.gitignore` إذا كنت تستخدم Git
- 🌐 توافق مع واتساب Business و WhatsApp العادي
- 📱 رقم واحد لكل جلسة

---

## ❓ الأسئلة الشائعة

### هل أحتاج QR في كل تشغيل؟
❌ لا، فقط في أول مرة. بعدها الجلسة محفوظة.

### كيف أعيد المسح من جديد؟
احذف مجلد `tokens/royal_session/` وشغّل السيرفر مرة أخرى.

### هل يعمل مع أرقام متعددة؟
نعم، لكن كل رقم يحتاج `session` منفصل.

### كيف أوقف السيرفر؟
اضغط `Ctrl + C` في Terminal

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. تأكد من تثبيت Node.js (v14 أو أحدث)
2. تأكد من تثبيت المكتبات: `npm install`
3. تحقق من اتصال الإنترنت
4. راجع رسائل الأخطاء في Console

---

## 📄 الترخيص

MIT License - حر للاستخدام الشخصي والتجاري

---

✨ **صُنع بـ ❤️ للمطورين العرب**
"# whatsapp-bot" 
